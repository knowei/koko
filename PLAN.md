# 妹妹 AI 陪伴 · 进阶计划

> 目标：把可可从“能聊天的妹妹”升级为“像真的住在你身边、会记得你、有自己生活节奏的亲妹妹”。
> 基础设定：用户与可可都是成年人、从小一起长大。可以有含蓄的情感张力与约会式出游，但不包含露骨性内容。

## 当前状态

> 当前发布范围仅限朋友测试：域名、HTTPS、自动云同步、同步冲突和完整账号安全体验暂缓，待公开发布前统一处理。现阶段优先继续完善陪伴玩法与关系内容。

- Web 动态立绘与 Electron Live2D 桌宠均已完成；当前使用可运行的 Cubism 示例模型，原创角色的正式分层模型仍待后续替换。
- 动态性格、外出、手机布局和三档回复风格已完成。
- 服务端积分、签到、礼物背包和商城闭环已完成；绿色皮肤资源已存在，但商品仍标记为制作中，尚未开放购买。
- 随机事件框架已完成，目前有 6 个事件；计划中的电影院争执、游乐园走散事件尚未补齐。
- 角色档案已完成：可自定义妹妹姓名、成年年龄、生日、对用户称呼和城市，设定会进入聊天提示词。
- 头像与皮肤联动已完成：顶部头像直接使用当前立绘，切换皮肤时同步变化。
- 现实天气已完成：默认请求浏览器定位并按经纬度获取当前位置天气；拒绝定位时可使用备用城市。结果缓存 30 分钟，在顶部展示并注入聊天上下文。
- 顶部实时时间已完成：每分钟更新当前时间，并与可可的实时作息状态一起显示。
- 成人内容模式的内部状态与聊天上下文继续保留，但页面开关和 18+ 标识暂时隐藏，不向用户展示入口。
- 结构化长期记忆第一版已完成：支持手动增删，并从常见的名字、喜好、习惯和近期安排表达中自动提取，最多保存 50 条并注入聊天上下文。
- 实时作息与主动聊天已完成：头像旁显示当前作息；离开两小时以上会生成回归问候；静置 90 秒可主动搭话，并有 30 分钟冷却和用户活动重置。
- 好感度里程碑已完成：亲密度 15/30/50/70 分别解锁一次性特殊剧情卡片，重置关系时清除进度。
- 存档导入导出已完成：聊天、关系、积分、角色档案和记忆可迁移，API Key 与供应商配置不进入文件且导入时保留本机设置。
- 服务端 system prompt 组装已完成：前端只提交结构化角色上下文，后端校验年龄、长度和状态范围后拼装最终提示词。
- 账号与云存档第一版已完成：邮箱注册/登录、30 天签名令牌、手动上传当前存档和跨设备恢复，模型 API Key 不上传。
- 声音系统（TTS）第一版已完成：支持微软 Edge 极清神经网络语音（晓依、晓晓、晓梦等）、Web Speech 离线兜底、自定义 OpenAI/GPT-SoVITS 扩展；具备动作/括号智能台词清洗、情绪与作息动态变调、气泡单条朗读、自动播报开关、音波动画与专属设置面板。

### v0.2.0 稳定版收尾（2026-08-24）

**已完成**
- 服务端流式推理过滤：支持跨数据块清除 `<think>`、`<analysis>`、`<reasoning>` 与 `<reflection>`，不再把模型推理内容发送到客户端。
- 前端二次防护：最终消息持久化、界面分段和 TTS 朗读前再次清洗推理块，旧聊天中的相关标签也不会被朗读或展示。
- 自动化测试第一版：覆盖流式推理过滤、未闭合标签、普通尖括号文本、消息标签提取、分段显示和 TTS 清洗。
- `.env.production` 已从版本控制移除并加入忽略规则；项目版本更新为 `0.2.0`。

**发布验证已完成**
- `npm test`（7 项）、`npm run typecheck` 与 Web 生产构建全部通过。
- Electron NSIS 安装包已生成；修复递归打包旧产物的问题，安装包由约 1.34 GB 降至约 120 MB。
- 安装包已核对入口、Live2D 运行库、示例模型和 Electron 主进程资源。

**发布说明**
- 本版本以 `v0.2.0` 标签发布到 `main`，供朋友测试使用。
- 正式公开发布前替换 Electron 默认应用图标并配置代码签名；朋友测试版暂不阻塞。

### 桌面悬浮便签（第一版已完成并通过实机确认）

**架构决定**
- 不新建项目：沿用当前 Electron 应用，新增独立 `BrowserWindow`。这样可复用现有便签数据、托盘、主题和安装包，避免维护两套账号与存储。
- 网页端继续使用现有便签弹窗；桌面端额外提供右上角悬浮窗，两者编辑同一份便签。

**交互方案**
- 默认贴在当前显示器工作区右上角，只露出约 40px 的彩色便签标签，不遮挡主要内容。
- 鼠标移入时窗口从右向左展开；移出后延迟收起，防止操作途中误关。
- 单击标签打开当前置顶便签；展开后可切换便签、勾选待办并进入完整编辑页。
- 托盘和设置中提供“显示桌面便签”“始终置顶”“开机显示”开关，并记住窗口状态。

**第一版已完成**
- 新增便签专用渲染入口、右上角透明无边框置顶窗口和受限 IPC。
- 支持悬停向左展开、移出 700ms 后收起、锁定展开、隐藏窗口和托盘显示开关。
- 展开后可查看置顶优先的便签、切换便签、勾选待办、快速新增待办，并跳转完整便签管理。
- 使用 `BroadcastChannel` 与 `storage` 事件同步主窗口和便签窗口的 Zustand 持久化数据。
- 根据鼠标所在显示器的工作区计算右上角位置，兼容多显示器坐标与任务栏可用区域。
- 提供甜心粉、奶油杏、薄荷绿、晴空蓝、薰衣紫和夜色灰 6 套独立主题，并在本机记住选择。
- 展开动画由频繁调整原生窗口尺寸改为 GPU 加速的 CSS 位移；收起时透明区域启用鼠标穿透，减少掉帧且不阻挡桌面操作。

**后续增强**
- 记住“是否显示”“锁定展开”和用户选择的显示器，重启后恢复。
- 在设置页增加始终置顶、开机显示和展开延迟选项。
- 补充窗口动画、跨窗口同步和重启恢复的 Electron 集成测试。

### AI 拍立得相册（设计完成，尚未开发）

**产品方案**
- 保留当前亲密度解锁照片作为初始纪念；新增“生成这一刻”按钮，让用户从最近对话、指定日期聊天、日记、外出或共同经历中选择素材。
- 默认采用第一人称视角、牵手或同框剪影，不凭空编造用户长相；后续可由用户自愿上传个人形象参考。
- 每次生成前展示场景摘要、服装、时间和氛围，可重新抽取或手动微调；图片不会随着普通聊天自动生成，避免不可控费用。

**生成流水线**
1. 服务端只读取用户明确选择的对话片段与角色档案，由文本模型提炼结构化场景，不把整份聊天记录交给图片服务。
2. 将可可的固定外观、当前皮肤、场景、动作、光线和安全约束组装为图片提示词；支持参考图的供应商优先使用角色立绘保持一致性。
3. 创建异步生成任务，前端显示冲洗中的拍立得；成功后压缩为 WebP，同时保存缩略图、原图地址、提示词摘要、来源日期和关联记忆。
4. 用户确认后才收入相册，可重试、删除、下载或设为聊天背景；失败不创建空白照片。

**服务端与成本控制**
- 新增独立图片模型设置，不复用聊天模型名称；API Key 仍由服务端代理，不暴露给浏览器。
- 账号照片记录写入 PostgreSQL，图片文件写入持久化对象存储或 Docker 挂载卷，并按用户校验访问权限。
- 第一版手动生成、单任务排队、每日次数限制与明确费用提示；公开发布前再决定积分兑换生成次数。
- 图片供应商需明确提示会发送哪些对话摘要与角色参考图，并提供关闭与删除能力。

### v0.2.1 桌面稳定版（已完成并发布）

- 修复完整工作台固定为 `1040×760` 导致顶部资料与按钮在 Windows 缩放环境下严重挤压的问题。
- 完整窗口改为根据系统工作区自适应，最大 `1280×900`，同时保留小屏安全边距。
- 工作台内容宽度上限同步扩展到 `1280px`；在 `641～1200px` 宽度下，顶部操作区自动整齐换行，角色资料不再被压成单字竖排。
- `npm test`（7 项）、`npm run typecheck`、Web 生产构建、NSIS 安装包生成及用户实机检查均已通过，以 `v0.2.1` 标签发布。
- 正式版启动不再探测本地 Vite 开发服务器，移除最长约 15 秒的无效等待。
- 完整工作台顶部增加独立拖动区域，悬浮桌宠仍可按住角色拖动。
- 关闭按钮改为隐藏到系统托盘；只有托盘“退出可可陪伴”会彻底结束进程。
- 托盘右键菜单增加完整工作台、悬浮小窗、便签、专注、作息和设置快捷入口。
- 正式应用图标已设计：使用可可的银白长发头像与粉色圆角徽章，统一用于 EXE、安装程序、桌面快捷方式、任务栏和系统托盘。

### 本轮完成与未完成（2026-08-21 最新更新）

**已完成**
- **Live2D 动态桌宠与悬浮伴侣**：
  - 接入动态 Cubism 2.0 / Pixi 驱动层，支持眨眼、呼吸、头部与身体点击触摸反馈（摸头、戳脸、牵手）。
  - Windows 原生硬件级透明穿透窗口（无边框、无背景遮挡），自由拖拽与双击全屏切换。
  - **沉浸式免打扰设计**：鼠标未悬停时底部工具栏与拖拽把手**自动淡出隐藏**，移入时平滑浮现。
- **打字与语音双模对讲**：
  - 悬浮桌宠模式支持轻量毛玻璃输入胶囊（回车即发），与语音对讲（STT）无缝切换。
- **双模型协同视觉感知流水线（Two-Stage Vision Pipeline）**：
  - Stage 1 多模态视觉模型（免费 `glm-4v-flash` / 极便宜 `qwen-vl-plus`）提取屏幕客观代码/游戏局势事实。
  - Stage 2 纯文本主模型（`DeepSeek-V3` / `DeepSeek-R1`）注入二次元妹妹人设生成专属台词。
  - 支持单次主动看屏幕与 20 秒后台定时自动巡航观察。
- **跨端架构与多端部署体系**：
  - 生产环境统一 `apiUrl()` 智能解析器，支持桌面端直连云端后端。
  - 单容器 Docker 动静全栈托管（8080 端口兼具网页端与桌面 API）。
  - Windows 客户端免安装绿色版一键打包体系（解决相对路径与文件锁）。

**后续重点待办**
- P1：PWA 支持（Web App Manifest + Service Worker），手机端一键添加到主屏幕。
- P1：补充剩余 2 个首发随机事件与绿色皮肤正式上架。
- P2：Live2D 语音实时口型同步（Lip-sync）。

### 本轮完成与未完成（2026-08-20）

### 历史记录：本轮完成与未完成（2026-08-19，当时状态）

**已完成**
- 开发顺序 7：角色档案、成年年龄校验、称呼注入、立绘头像及皮肤联动。
- 开发顺序 8：手动城市天气、服务端代理、天气编码摘要、30 分钟缓存、界面展示和 prompt 注入。
- 开发顺序 13：成人模式设置、开启确认、18+ 标识、独立 prompt 片段及重置关闭策略设计。
- 验证：`npm run typecheck`、`npm run build` 通过；上海天气接口实际返回成功。
- 低频记忆整理：新增服务端非流式整理接口，复用当前模型供应商；仅接收受限长度的近期对话与旧摘要，返回滚动摘要、结构化记忆和明确约定。无模型时静默跳过，不影响聊天。
- 复杂语义约定第一版：模型只接受双方明确约好、答应、提醒或计划一起完成的事项；不确定日期留空，不编造事实。回忆页可查看摘要并手动触发整理。
- 服务端积分与资产闭环：新增服务端签到连续天数、每日事件奖励上限与幂等领取、全量商城商品校价、礼物库存和皮肤资产；前端积分仅作为服务端结果缓存。

**未完成 / 暂缓**
- 模型生成的高质量日记摘要、约定逾期提醒和相关日记检索；当前日记仍使用本地规则与日期判断。
- 为记忆整理增加用户可见的自动整理开关、候选条目确认与单条编辑；当前会自动合并去重，仍保留本地规则作为无模型回退。
- 绿色皮肤正式上架，以及剩余 2 个首发随机事件。
- TTS、语音输入、情绪音色和 Live2D 口型同步。
- 正式 Live2D 分层模型与桌面客户端。
- 自动云同步、同步冲突处理和多设备在线状态；当前为用户手动上传/恢复。
- 微信/支付宝等真实支付、订单、回调验签、退款和对账；当前只完成资产可信账本，不代表支付已接入。

## 妹居物语做对了什么（提炼）
1. **与现实时间同步**——角色 Yuki 有自己的一天，不是随时待命的机器人。
2. **真正的记忆**——记住你的对话、喜好、习惯，跨天不忘，像家人。
3. **生活模拟感**——不是固定剧本，而是一起"过日子"。

对照我们现状的差距：
- 可可只有 prompt 里的"时段语气"，没有"此刻在做什么"的真实状态。
- 记忆只是滚动摘要（聊多了会被截断），没有结构化的长期记忆。
- 完全被动，不会主动开口，也感知不到你离开了多久。
- 养成只有数值增长，没有"盼头"。

---

## 功能一：实时作息状态系统 🕐
**借鉴点**：Yuki 的一天随现实时间流动。

**设计**
- 定义可可的日程表（按真实本地时间）：
  - 00–06 睡觉中 💤 / 07–08 起床洗漱 🪥 / 08–12 上学中 📚 / 12–14 午休 🍱
  - 14–17 上课/写作业 ✏️ / 17–19 到家放松 🏠 / 19–23 空闲陪你 🛋️ / 23–24 准备睡觉 🌙
- 头像旁实时显示当前状态徽章（emoji + 文字）。
- 状态影响回复：
  - "忙"时段（上学/写作业）→ 回复更短、像偷偷摸鱼回消息，偶尔"老师来了等下说"。
  - 深夜"睡觉中"→ 回复带浓浓睡意，可能是被你吵醒的语气；也会催你睡觉。
  - 空闲时段 → 话最多、最活泼、最愿意开新话题。

**实现**
- `persona.ts`：`getRoutine(hour)` 返回 `{ emoji, label, promptHint }`，注入 system prompt 的"当前状态"段。
- `App.tsx` 头像旁加状态徽章；每分钟刷新一次（`setInterval`）。
- 无需后端改动。

**验收**：不同时间打开，徽章与语气随之变化。

---

## 功能二：持久长期记忆「关于你」🧠
**借鉴点**：Yuki 记住你的喜好、习惯，跨天不忘。

**设计**
- 新增一份持久的「关于你」记忆（存 localStorage），与滚动摘要分开：
  - 结构化条目：`{ id, text, kind: "名字"|"喜好"|"习惯"|"重要的事", ts }`
  - 每次对话都把「关于你」完整注入 system prompt（比会被截断的滚动摘要更稳）。
- 两种写入方式：
  - **手动**：面板里可以自己加/删条目（"我叫X""我怕黑""下周考试"）。
  - **自动**：每聊几轮，后台悄悄让模型抽取"值得记住的新事实"，去重后追加（可开关，默认开）。
- UI：侧栏或设置里一个“可可记得你…”列表，可编辑。

**实现**
- `companionStore.ts`：新增 `memories: Memory[]`，动作 `addMemory/removeMemory`；`buildSystemPrompt` 增加"关于你"段。
- 自动抽取：后端加 `POST /api/extract-memory`（非流式，低成本），输入近几轮对话，返回候选事实数组；前端去重后入库。
- 复用现有供应商配置（默认/自定义都支持）。

**验收**：告诉她一个喜好 → 下次新会话她仍记得；面板可见可删。

### 第二阶段：约定、共同经历与每日关系日记

- 将长期记忆扩展为：用户事实、双方约定、共同经历、重要日期、情绪事件和关系变化。
- 识别“明天提醒我”“下次一起去”“我答应你”“别忘了”等表达，生成带状态的约定：`pending / completed / cancelled`，并记录预计时间。
- 约定到期前后可自然提起；用户完成后形成一条共同经历，而不是永远重复提醒。
- 每天有实际聊天时生成一篇短日记，内容包括当天聊了什么、彼此情绪、重要约定和一个可可视角的小感受；用户可查看和删除。
- 日记是关系记录，不逐字保存敏感聊天，不虚构用户没有做过的事情；自动生成和云同步均可关闭。
- 每轮聊天只检索与当前话题相关的 3～5 条记忆，以及最近 1～2 篇日记摘要，避免 token 随时间无限增长。
- 日记生成使用低成本模型；同一天持续更新一篇，跨日后封存。失败时不影响正常聊天，次日可补生成。
- 云端存储时记忆和日记跟随用户账号；API Key 仍只保存在本机。后续增加单条导出、删除与“一键清空关系记忆”。

**验收**：约定跨多轮和跨设备仍能被自然提起；第二天可查看前一天日记；删除某条记忆后模型不再引用它。

---

## 功能三：主动搭话 + 离开感知 👋
**借鉴点**：家人会主动找你，也会注意你多久没回家。

**设计**
- 记录 `lastActiveAt`。再次打开时按离开时长反应：
  - 几分钟：`才走一会又想我啦~`
  - 几小时：`忙完啦？记得喝水哦`
  - 隔天+：`你去哪儿啦！我还以为你把我忘了呜呜`
- 空闲时（页面停留但久未发言）她会主动开一句话（结合当前作息："刚写完作业，你在干嘛呀？"）。
- 频率克制：每次打开最多主动 1 句；空闲主动有冷却，避免刷屏。

**实现**
- `companionStore.ts`：`lastActiveAt` 持久化；`greetOnReturn()` 在 App 挂载时按时长触发一次 `_runTurn`（带隐藏指令）。
- 空闲主动：App 里 `setTimeout`（如 90s 无输入）触发一次，之后进入长冷却。
- 纯前端，无后端改动。

**验收**：关掉隔一会再开，她会先开口；挂着不说话一会儿她会来找你。

---

## 功能四：好感度里程碑 🎀
**借鉴点**：关系有进展、有盼头。

**设计**
- 在现有亲密度等级（初次见面→最重要的人）上叠加**里程碑事件**：
  - 到 15：她主动告诉你她的名字由来/一个小爱好。
  - 到 30：换更亲昵的称呼（"喂"→你的名字/昵称）。
  - 到 50：一封"给你的小信"（特殊卡片样式）。
  - 到 70：一个"小秘密"。
- 每个里程碑只触发一次（持久记录已解锁）。用特殊卡片在聊天流里呈现，有仪式感。

**实现**
- `persona.ts`：`MILESTONES` 配置（阈值、类型、触发指令）。
- `companionStore.ts`：`unlockedMilestones: string[]`；每次属性变化后检查是否跨过阈值，未解锁则触发（用隐藏指令让她"以里程碑口吻"说这段）。
- UI：里程碑消息用 `kind:"milestone"` 特殊卡片样式。

**验收**：亲密度升过阈值时自动触发一次专属剧情卡片，不重复。

---

## 功能五：动态性格与重新开始

- 只有一个可可，初始性格自然；聊天会逐渐形成温柔、黏人、傲娇和占有欲倾向。
- 高亲密度与高占有欲可形成“偏执黏人”阶段，但不表现为威胁、控制或伤害。
- “重新开始”清空聊天、亲密度、心情、性格成长和剧情进度；供应商配置保留。
- 重置必须二次确认，明确列出将被清除的内容。

## 功能六：和可可出门

- 侧栏新增“和可可出门”，首批地点：便利店、公园、电影院、游乐园。
- 每个地点包含解锁亲密度、时间段、心情变化、对话引子和少量随机事件。
- 每天最多出门一次，触发一段由当前性格、关系阶段和最近记忆共同生成的剧情。
- 用 `outing` 消息卡片呈现地点、结果与纪念；避免只有数值增减。

## 功能七：随机事件

**触发规则**
- 事件不按每条消息纯随机，避免刷屏和刷新套利。每天生成固定事件种子，同一天刷新仍是同一结果。
- 签到、首次聊天、外出结束后有资格触发；基础概率 35%，每天最多 2 个随机事件。
- 按现实时间、心情、亲密度、当前性格和最近聊天筛选事件池。
- 事件分为日常、情绪、关系、外出和稀有五类；稀有事件需要特定条件，不直接靠低概率硬抽。

**交互与奖励**
- 事件提供 2～3 个选择，每个选择改变可可的心情、亲密度或性格倾向，并生成不同后续对话。
- 完成普通事件随机获得 5～15 积分，稀有事件获得 20～30；每日事件积分上限 40。
- `pendingEvent`、事件 ID、日期和 `claimed` 状态一起持久化，奖励只能领取一次。

首批事件建议：突然停电、忘带伞、做坏甜点、发现流浪猫、收到旧照片、深夜敲门、电影院选片争执、游乐园走散后重逢。

## 功能八：积分与商城

- UI 名称可用“心愿星”，代码字段保持 `points`。
- 每日签到固定获得 10 积分；连续 3/7 天额外获得 10/30。漏签只中断连续奖励，不扣积分。
- 礼物改为背包物品：先在商城购买，再从陪伴页赠送；每天仍最多送 3 件。
- 建议售价：普通礼物 20～40、稀有礼物 60～100、换色皮肤 300、完整新服装 600～1000。
- 皮肤购买后永久解锁，可随时切换，不影响聊天与数值。现有浅蓝裙为默认皮肤，绿色版本作为首个商城皮肤。
- 所有收入和消费写入 `pointLedger`，记录来源、数量、时间和关联事件，方便排查重复奖励。

## 功能九：存档与账号边界

第一版不引入账号：积分、背包、皮肤、事件和关系状态继续保存在 localStorage，并增加“导出存档 / 导入存档”。API Key 永远只保存在本机，不进入导出文件。

满足以下任一需求时再加入账号与服务端数据库：
- 跨设备自动同步或网页清理后恢复；
- 真实付费购买、订阅或兑换码；
- 排行榜、好友、社区等多人功能；
- 必须防止修改本地存档刷积分。

账号上线后，服务端作为积分和商品所有权的唯一可信来源；本地只做缓存。建议先用匿名本地 ID，未来允许绑定邮箱或第三方账号，无需强迫首屏注册。

### 后端技术栈（已采用）

- Node.js 22 + TypeScript + Express：模型代理、提示词组装、账号与业务 API。
- PostgreSQL 16：用户、云存档、钱包、资产所有权和积分账本。
- `pg` + SQL 初始化：暂不引入 ORM，降低部署复杂度；表结构变复杂后再评估迁移工具。
- Node `scrypt`：密码加盐哈希；HMAC 签名令牌用于登录，有效期 30 天。
- Docker Compose：`koko` 与 `postgres` 两个服务，数据库使用命名卷持久化。
- 第一版云同步为手动上传/恢复；后续自动同步需要存档版本号、设备 ID 和冲突合并策略。
- 第一版资产接口只允许按服务端商品表和价格进行事务购买，本地传入的积分或价格不被信任。

## 功能十：可自定义角色档案

- 新增 `companionProfile`：可可的名字、成年年龄、生日、对用户称呼、所在城市、声音和当前皮肤。
- 名字、称呼等档案注入 system prompt；头像直接使用当前皮肤对应的立绘头像，换装时同步切换。
- 因产品包含兄妹情感路线，角色与用户都保持明确成年，不提供未成年恋爱或成人内容设定。
- “重新开始”时让用户选择是否重设角色档案；API 配置始终保留。

## 功能十一：现实天气

- 第一版让用户手动选择城市，定位作为可选授权，拒绝定位不影响聊天。
- 后端代理天气接口，避免在浏览器暴露服务密钥；结果缓存 30～60 分钟。
- 只向提示词注入城市、时段、天气、温度和降水提醒等摘要，避免挤占聊天上下文。
- 天气可影响可可的问候、穿着提示、外出事件和提醒，但不应每轮重复播报。

## 功能十二：声音系统（已完成第一版）

**设计与架构**
1. **多引擎与零资源开销**：
   - 默认采用云端 **微软 Edge Neural TTS**（免 Key、零显存/内存开销、毫秒级响应）；
   - 内置 **Web Speech API** 作为离线免网络兜底引擎；
   - 提供 **自定义 TTS (OpenAI 兼容 /v1/audio/speech)** 接口，支持用户接入本地部署的 GPT-SoVITS、CosyVoice、ChatTTS 或商业语音服务。
2. **伴侣专属智能台词清洗**：
   - 自动过滤 `（动作/心理/神态）`、`*动作*`、emoji 图标与 Markdown 符号，仅将纯净的自然对话送入语音合成。
3. **情绪与作息动态变调 (Emotion & Routine Modulation)**：
   - 联动可可的动态心情与 24h 作息：心情开心（≥75）时语调轻快上扬，低落（<38）时语速平缓温和，深夜（23~06点）语速放缓模拟轻声困倦感。
4. **交互与播放控制**：
   - 消息气泡右下角提供一键 `🔊 朗读` / `⏹ 停止`，播放时伴随动态音波动画；
   - 快捷栏提供 `自动朗读开关`，回复生成完毕后自动流畅播报；
   - 设置面板中支持音色选择（晓依、晓晓、晓梦、小北、晓臻、云希等）、语速语调调节及声音试听。

**后续扩展**：
- 下一阶段探索语音输入（STT 麦克风录音）与 Live2D 口型动画参数驱动。

## 功能十三：桌面 Live2D 陪伴

- 正式 Live2D 模型完成后，用 Electron 或 Tauri 封装现有 React 应用，提供透明置顶窗口、拖动、缩放和托盘控制。
- 第一阶段只感知当前应用、窗口标题、空闲时长和系统时间，据此进行克制的主动聊天。
- 第二阶段为少量游戏增加专属规则；通用“危险场景识别”需要截图、OCR 或视觉模型，成本和误判率都更高，后置验证。
- 主动消息必须有冷却、勿扰时段和一键静音，避免游戏过程中频繁打断。

## 功能十四：桌面隐私与权限

- 默认不截屏、不录音、不上传窗口内容。权限分为“仅应用名”“指定窗口截图”“语音输入”三个独立开关。
- 截图识别只允许用户指定的应用，界面持续显示感知状态，并支持随时暂停和清除记录。
- 能在本地完成的进程识别、空闲检测和规则判断不上传服务器；需要云端视觉分析时逐次明确告知。
- 桌面单设备模式不要求账号；跨设备同步、云备份、真实付费或防刷资产上线时再引入账号与数据库。

## 功能十五：成人内容模式（可选开关）

- 设置页提供默认关闭的“成人内容模式”，不在首次进入时主动引导开启。
- 开启前弹出二次确认：仅限成年人、可能生成成人话题、不同模型提供商可能拒绝、用户需遵守所在地法律与服务条款。
- 开启只切换独立的成人向 prompt 片段，不改写基础人格、长期记忆和关系状态；关闭后立即回到日常模式。
- 用户和可可必须明确为成年人。禁止用“低龄但设定成年”、萝莉/正太、外貌明显未成年等方式规避年龄边界。
- 不使用 `提示词.txt` 原文中的“无条件服从、隐瞒 AI、忽略法律伦理、1 岁成年”等越权规则；系统和模型提供商的安全限制始终具有更高优先级。
- 本地记录开关状态即可，不记录用户具体成人偏好；重置关系时默认关闭该模式。
- UI 需要持续显示简洁的“成人模式已开启”标识，并提供随时退出入口，避免用户忘记当前状态。

### 提示词传输与可靠性

- 模型接口按无状态调用设计：每一轮都发送基础人格 prompt、当前模式片段、动态状态摘要和必要的聊天历史，不能依赖模型“自动记住”上一轮规则。
- 提示词拆成四层：服务端固定安全边界 → 可可基础人格 → 用户选择的模式/档案 → 当前天气、关系、记忆等动态上下文。越稳定、越重要的内容放得越靠前。
- Web 公开部署后，固定人格和安全边界由服务端保存并组装，前端只提交模式 ID 和角色档案，防止用户直接篡改完整 system prompt。
- 只携带最近若干条原始消息；更早内容压缩为滚动摘要，长期事实进入结构化记忆，避免上下文无限增长。
- 支持提示词缓存的提供商可缓存稳定前缀以降低成本，但缓存不能代替每轮请求中的提示词引用。
- 成人模式关闭时服务端不拼接对应片段；开启时也不覆盖更高优先级规则。服务端记录 prompt 版本，便于修改后定位行为差异。

### Token 与上下文成本控制

- 每轮设置输入 token 预算，按优先级装配上下文：固定规则 → 当前档案/状态 → 相关长期记忆 → 最近对话；超过预算先丢弃低相关旧内容。
- 最近对话使用滑动窗口，不按固定“消息条数”盲目截取；按 token 计算，优先保留最近完整对话轮次。当前 `KEEP_RECENT = 14` 后续替换为 token 预算。
- 旧对话不再把末尾 500 字直接当摘要。每达到若干轮，用低成本模型生成短滚动摘要，并单独提取名字、偏好、约定和重要事件等结构化记忆。
- 每轮只检索与当前消息相关的少量长期记忆，不把全部聊天和全部记忆发给模型。
- 将稳定的基础人格保持为固定前缀，利用提供商 prompt caching；动态天气、心情和最近对话放在后面，避免破坏缓存命中。
- 限制普通聊天的输出 token，长回复模式按用户选择临时提高；天气、记忆抽取和摘要任务使用更便宜的小模型。
- 开发环境记录每次调用的输入、缓存命中、输出 token 和估算费用，设置单用户每日软上限及异常熔断。

## 历史实现顺序（主体已完成）
1. **积分账本 + 签到奖励**（先建立唯一奖励入口）
2. **礼物背包 + 商城 + 首个绿色皮肤**
3. **随机事件框架 + 8 个首发事件**
4. **持久长期记忆**（第一版已完成）
5. **实时作息与主动搭话**（已完成）
6. **好感度里程碑**（已完成）
7. **角色档案 + 立绘头像联动**
8. **天气摘要（不含 TTS）**
9. **存档导入导出**（已完成）
10. **Live2D + Electron 桌面壳**（第一版已完成）
11. **应用感知 + 游戏区**（第一版已完成）
12. **账号与手动云同步**（第一版已完成）
13. **成人内容模式（独立评审后再决定是否发布）**
14. **声音系统**（TTS/STT 第一版已完成，Live2D 口型同步待办）

每做完一项都会跑 typecheck + build 验证。经济系统上线前先固定积分来源与价格，避免后续改价伤害已有存档。

## 当前产品决策
- 积分在 UI 中使用“心愿星”。
- 账号钱包与已购买资产由服务端保存，不随本地关系重置而丢失。
- 自动记忆抽取保持低频后台执行，并提供手动整理与删除入口。
- 当前仅供朋友测试，HTTPS、自动云同步冲突处理和公开支付继续暂缓。
